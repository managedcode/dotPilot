<Page x:Class="DotPilot.Presentation.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:DotPilot.Presentation.Controls"
      Background="Transparent"
      FontFamily="{StaticResource AppBodyFontFamily}">
  <Grid AutomationProperties.AutomationId="SettingsScreen"
        MaxWidth="1380"
        RowSpacing="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid AutomationProperties.AutomationId="SettingsScreenHeader">
      <StackPanel Spacing="4">
        <TextBlock AutomationProperties.AutomationId="ProvidersPageTitle"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Text="{Binding PageTitle}" />
        <TextBlock FontSize="12.5"
                   Foreground="{StaticResource AppMutedTextBrush}"
                   Text="{Binding PageSubtitle}"
                   TextWrapping="WrapWholeWords" />
      </StackPanel>
    </Grid>

    <controls:SettingsShell Grid.Row="1" />
  </Grid>
</Page>
