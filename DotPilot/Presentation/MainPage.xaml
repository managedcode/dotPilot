<Page x:Class="DotPilot.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:DotPilot.Presentation.Controls"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{StaticResource AppShellBackgroundBrush}"
      FontFamily="{StaticResource AppBodyFontFamily}">
  <Grid utu:SafeArea.Insets="VisibleBounds"
        AutomationProperties.AutomationId="MainChatScreen">
    <Canvas>
      <Ellipse Width="540"
               Height="473"
               Canvas.Left="-207"
               Canvas.Top="482"
               Fill="{StaticResource AnalyticsAvatarBrush}"
               Opacity="0.18" />
      <Ellipse Width="887"
               Height="777"
               Canvas.Left="1007"
               Canvas.Top="-264"
               Fill="{StaticResource UserAvatarBrush}"
               Opacity="0.14" />
    </Canvas>

    <Grid Margin="12"
          AutomationProperties.AutomationId="MainChatLayout"
          ColumnSpacing="20"
          RowSpacing="18">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="260" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="304" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <controls:ChatSidebar Grid.RowSpan="2" />

      <controls:ChatConversationView Grid.Column="1" />

      <controls:ChatComposer Grid.Column="1"
                             Grid.Row="1"
                             Margin="0,0,0,6" />

      <controls:ChatInfoPanel Grid.Column="2"
                              Grid.RowSpan="2" />
    </Grid>
  </Grid>
</Page>
