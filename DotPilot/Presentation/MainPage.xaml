<Page x:Class="DotPilot.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:DotPilot.Presentation.Controls"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{StaticResource AppShellBackgroundBrush}"
      FontFamily="{StaticResource AppBodyFontFamily}">
  <Grid utu:SafeArea.Insets="VisibleBounds"
        AutomationProperties.AutomationId="ChatScreen">
    <Canvas IsHitTestVisible="False">
      <Ellipse Width="540"
               Height="473"
               Canvas.Left="-207"
               Canvas.Top="482"
               Fill="{StaticResource AnalyticsAvatarBrush}"
               Opacity="0.18" />
      <Ellipse Width="887"
               Height="777"
               Canvas.Left="1007"
               Canvas.Top="-264"
               Fill="{StaticResource UserAvatarBrush}"
               Opacity="0.14" />
    </Canvas>

    <Grid Margin="12"
          AutomationProperties.AutomationId="ChatLayout"
          ColumnSpacing="20"
          RowSpacing="18">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="280" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="320" />
      </Grid.ColumnDefinitions>

      <controls:ChatSidebar />

      <Grid Grid.Column="1"
            RowSpacing="18">
        <Grid.RowDefinitions>
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:ChatConversationView />
        <controls:ChatComposer Grid.Row="1" />
      </Grid>

      <controls:ChatInfoPanel Grid.Column="2" />
    </Grid>
  </Grid>
</Page>
